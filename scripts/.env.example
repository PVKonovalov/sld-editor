# Copy this to scripts/.env (gitignored) and fill in your own deploy
# target — scripts/deploy.sh sources it automatically.
DEPLOY_HOST=root@192.168.1.2
DEPLOY_SSH_KEY=~/.ssh/id_rsa_test

# Optional — defaults to /usr/lib/sld-editor if unset.
# DEPLOY_REMOTE_DIR=/usr/lib/sld-editor
